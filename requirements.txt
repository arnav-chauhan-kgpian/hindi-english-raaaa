# Builderr Speech-to-Text — FINAL submission dependencies.
# Target: Linux + NVIDIA GPU, offline at scoring. Validated on Tesla T4 (CUDA 12.x).
# qwen-asr[vllm] resolves the exact torch/vllm/transformers combo; the floors below match
# the validated stack. Warm the cache once online, then run fully offline (HF_HUB_OFFLINE=1).

faster-whisper>=1.0          # fast English / Indian-English path (CTranslate2, GPU int8_float16)
qwen-asr[vllm]==0.0.6        # Hinglish recognizer wrapper (provides Qwen3ASRModel + vLLM backend)
vllm>=0.8                    # Hinglish inference backend (Triton attention)
torch>=2.6                   # pulled/pinned by qwen-asr[vllm]; GPU build
transformers>=4.46           # required by qwen-asr (model + processor)
accelerate>=0.30             # model loading helper
huggingface_hub>=0.24        # local-cache-first model resolution (local_files_only=True)
ctranslate2>=4.5             # faster-whisper runtime
sentencepiece>=0.2.0         # tokenizer dependency
safetensors>=0.4.0           # weights format
psutil>=5.9                  # memory/diagnostics
soundfile>=0.12              # audio decode (wav/flac) for load_audio()
